Config = {
  -- Disable desktop animations, fade effects and cursor shadow while a game is running
  DisableDesktopEffects = true,

  -- Set CPU thread affinity based on the configured Core-Binding rules for each game
  SetCpuAffinity = true,

  -- Hide console window at startup (Windows only)
  HideConsole = true,

  -- If true, non-primary monitors will be disabled while a game is running and restored afterwards
  DisableNonPrimaryDisplays = true
}
